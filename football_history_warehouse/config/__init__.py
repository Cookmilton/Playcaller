"""
Operational configuration for the warehouse process (not football rules).

Holds environment-driven settings: storage locations, batch sizes, feature
flags for ingest modes, etc. League-specific *rules* live under ``rules``.
"""
