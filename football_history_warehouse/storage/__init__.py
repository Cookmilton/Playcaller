"""
Storage: durable persistence for normalized history.

The database engine, migrations, and repository implementations will live
under ``storage.database`` and ``storage.repositories``. Query-heavy read
paths may delegate to ``query`` for a stable consumer API.
"""
