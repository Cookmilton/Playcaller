"""
Per-source adapters (skeleton).

Each submodule or class will encapsulate one vendor or file format, emitting
a consistent *raw* payload type for normalization. Add sources without
touching unrelated leagues.
"""
